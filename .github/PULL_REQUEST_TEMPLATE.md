## What does this PR change?

<!-- One or two sentences. Reference issues with "Fixes #N". -->

## How was it tested?

<!-- Tests run on the anttest simulator need no hardware. Tick what applies. -->

- [ ] `make test` passes
- [ ] `make test-race` passes (if concurrency touched)
- [ ] New logic is covered by unit tests
- [ ] Verified against real ANT hardware (if protocol behaviour changed)
- [ ] Fuzz targets still pass (`make fuzz`, if parsers touched)
- [ ] Docs updated (README / package docs / TODO.md, if user-facing)

## Notes for reviewers

<!-- Anything non-obvious: protocol spec references, hardware quirks, trade-offs. -->
